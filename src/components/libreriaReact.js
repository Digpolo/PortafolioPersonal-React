//Actualización de estado en formulario

/*  

const [form, setForm] = useState({});

const handleChange = (e) =>{
        setForm({
            ...form,
            [e.target.name]:e.target.value,
        });
    };

    -----en el objeto-----
    value={form.email} ------aqui va el bombre de la varible.nombre del objeto
           onChange={handleChange}/>


*/